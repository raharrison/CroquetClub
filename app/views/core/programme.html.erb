<h1><%= @title %></h1>

<article id="events">
	<% @events.each do |event| %>
		<section>
			<h2><%= event.title %> (<%= event.date.to_formatted_s(:long) %>)</h2>
			<p>
				<%= event.description %>
			</p>
		</section>
	<% end %>
</article>
