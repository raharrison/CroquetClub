<h1>Book a Lawn</h1>

<p>Book a lawn at the club.</p>

<p>All bookings are for two hours.</p>

<% @bookings.each do |lawn, booking| %>
    <section>
        <h3>Lawn: <%= lawn %></h3>
        <% booking.each do |b| %>
            <article id="<%= b.id %>">
                <p><%= b.date.to_formatted_s(:long) %> at <%= b.time.to_formatted_s(:time) %></p>
                <p class="booked"><%= b.booked ? "Not Available" : "Available" %></p>
                <%= button_to "Book this Slot", update_booking_path(booking_id: b), :disabled => b.booked? %>
            </article>
        <% end %>
        <br>
    </section>
<% end %>

<%= link_to "Reset all Lawns", reset_bookings_path %>